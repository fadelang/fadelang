OutStream: trait {
  println(printable: Printable = "");
};

InStream: trait;

stdout(): -> OutStream {}

stderr(): -> OutStream {}

stdin(): -> InStream {}


println(stream: OutStream = stdout(), printable: Printable = "") {
  stream->println(string);
}
